package com.menegasso.projetobackendsenior.domain.model.person;

/**
 * Represents possible gender values for a Person.
 * This enum is part of the domain model and remains free
 * of any framework-specific annotations (Clean Architecture).
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
